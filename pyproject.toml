[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "tonie"
version = "0.1.0"
dependencies = [
  "protobuf"
]
requires-python = ">=3.9"

[project.scripts]
tonie-skip = "tonie:skip"
tonie-swap = "tonie:swap"
tonie-export = "tonie:export"
